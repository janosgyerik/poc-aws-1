Examples using DynamoDB with Python
===================================

Source the required environment variables:

    . ../../env.sh

Put an item into the configured DynamoDB table (see also -h, --help):

    ./put.py

List items in the configured DynamoDB table:

    ./list.py

